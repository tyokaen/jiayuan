<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jiayuan.library">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="MainActivity" />
        <activity
            android:name=".login"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".Main"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".showSpinner"
            android:label="@string/title_activity_show_spinner" />
        <activity android:name=".book_info" />
        <activity android:name=".Login_provider" />
        <activity android:name=".Info_Register" />
        <activity android:name=".search_result" />
        <activity android:name=".provider_show_goods"></activity>
    </application>

</manifest>